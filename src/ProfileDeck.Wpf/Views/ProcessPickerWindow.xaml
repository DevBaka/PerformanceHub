<Window x:Class="ProfileDeck.Wpf.Views.ProcessPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Programm auswählen" Height="500" Width="620" WindowStartupLocation="CenterOwner"
        Background="#1E1F22" Foreground="#E3E5E8">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Aktualisieren" Click="Refresh_Click"/>
            <Button Content="Datei wählen (.exe)..." Click="BrowseFile_Click"/>
            <TextBox x:Name="FilterBox" Width="220" Margin="16,2,2,2" TextChanged="FilterBox_TextChanged"
                     ToolTip="Nach Name filtern"/>
        </StackPanel>

        <ListView Grid.Row="1" x:Name="ProcessListView" MouseDoubleClick="ProcessListView_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="" Width="28">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Image Source="{Binding Icon}" Width="18" Height="18"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="180"/>
                    <GridViewColumn Header="Pfad" DisplayMemberBinding="{Binding Path}" Width="360"/>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Übernehmen" Width="90" Click="Take_Click" IsDefault="True"/>
            <Button Content="Abbrechen" Width="90" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
